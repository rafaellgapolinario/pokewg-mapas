# ─────────────────────────────────────────────────────────────
# Ativos grandes: NAO entram no git.
# Vem por `npm run fetch-assets` (baixa do CDN publico do jogo).
# ─────────────────────────────────────────────────────────────

# Mapas (325 arquivos, ~198 MB). Ignora tudo em dados/map/ ...
dados/map/*.json
# ... MENOS os 4 arquivos de suporte, que sao pequenos e SAO o alvo do trabalho:
!dados/map/manifest.json
!dados/map/offsets.json
!dados/map/draworder.json
!dados/map/collision.json

# Atlas de sprites (41 MB de webp/png) - arte do jogo, so cache local
dados/atlas/*
!dados/atlas/.gitkeep

# Exportacoes do visualizador (o dev gera, revisa e so entao commita o que quiser)
saida/

# Node / editores / SO
node_modules/
npm-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/

# Nunca, em hipotese alguma
.env
.env.*
*.pem
*.key
id_rsa*

# Folhas de contato e catálogos: são GERADOS (folha.js, busca.js, zoom-fam.js) e
# pesam MBs. Refazem em segundos; o que vale versionar é o script que os gera.
visualizador/folha.html
visualizador/catalogo.html
visualizador/familias.html
visualizador/zoom-fam-*.html

# EXCEÇÃO: os mapas DESENHADOS PELO EKOO entram, sim. A regra acima existe pros
# 198 MB de mapa do jogo, não pro nosso trabalho — que sem isto só tem backup
# local e se perde junto com a máquina.
# Os 00-base-cp / 00-base-market ficam DE FORA de propósito: são recorte do
# cerulean, e este repositório é público.
!dados/map/city-01.json

# ─────────────────────────────────────────────────────────────
# O QUE É NOSSO ENTRA NO GIT.
# A regra acima ignora ativos grandes porque eles voltam com
# 
> pokewg-mapas@1.0.0 fetch-assets
> node scripts/baixar-assets.mjs

· cerulean.json ja existe (use --forcar pra rebaixar)
· viridian.json ja existe (use --forcar pra rebaixar)
· abra.json ja existe (use --forcar pra rebaixar)

0 mapa(s) baixado(s), 0.0 MB

Os mapas presentes usam 35 de 41 paginas de atlas.

Atlas ja estava completo (nada a baixar).

Pronto. Agora rode:  npm run dev. Estes não voltam de lugar nenhum:
# são mapas que a gente montou e arte que a gente desenhou.
# Somados dão menos de 100 KB.
# ─────────────────────────────────────────────────────────────
!dados/map/00-*.json
!dados/map/GYM-*.json
!dados/map/city-*.json
!dados/map/ekoo-*.json
!dados/atlas/nossas/
!dados/atlas/nossas/*.png
